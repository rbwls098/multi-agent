# Week 5. 도구를 쓰는 에이전트 기초

> 학부 운영안 기준 축약본
> 원본 심화 내용: `docs/ch5.md`

## 학습 목표

- 에이전트가 도구를 선택하는 기본 원리를 설명한다
- 도구 설명과 입력 검증이 왜 중요한지 이해한다
- 구조화된 출력의 필요성을 설명한다
- 간단한 도구 호출 에이전트를 실행하고 결과를 점검한다

---

## 이번 주 운영 원칙

- 이번 주의 목표는 "복잡한 프레임워크 비교"가 아니라 "도구를 안정적으로 쓰는 에이전트 만들기"이다
- Tool Search와 Agents SDK 상세 비교는 심화 읽기로 돌린다
- 실습은 작은 도구 2~3개를 연결하는 수준에서 끝낸다

### 필수 범위

- 도구 선택의 원리
- 좋은 도구 설명 작성
- 입력 검증
- 구조화된 출력
- 간단한 도구 호출 실습

### 심화 읽기

- Tool Search
- Programmatic Tool Calling의 세부 제어
- OpenAI Agents SDK와 LangChain 비교

---

## 5.1 에이전트는 어떻게 도구를 고르는가

- 에이전트는 마법처럼 도구를 고르는 것이 아니다
- 실제로는 모델이 도구의 이름, 설명, 입력 형식을 보고 선택한다

도구 선택이 흔들리는 대표적인 경우:

- 설명이 모호함
- 비슷한 도구가 여러 개 있음
- 입력 형식이 불분명함

좋은 도구 설명의 조건:

- 무엇을 하는지 분명해야 함
- 언제 써야 하는지 분명해야 함
- 어떤 입력을 받는지 보여 줘야 함

---

## 5.2 도구 설명을 잘 써야 하는 이유

예를 들어 `get_weather`와 `get_coordinates`가 함께 있다면 모델은 다음을 알아야 한다.

- 도시 이름만 있을 때는 먼저 좌표를 구해야 함
- 좌표가 있으면 바로 날씨를 조회하면 됨

즉, 도구 설명은 사람이 읽는 문서이면서 동시에 모델이 읽는 실행 규칙이다.

```python
@tool
def get_weather_tool(latitude: float, longitude: float) -> str:
    """위도와 경도를 받아 현재 날씨를 조회한다."""
```

---

## 5.3 입력 검증

에이전트가 만든 입력은 항상 올바르지 않다.

자주 발생하는 문제:

- 필수 값 누락
- 타입 오류
- 범위 오류

이번 주에는 Pydantic 같은 검증 도구를 사용해 최소한 아래를 확인한다.

- 위도와 경도 타입
- 허용 범위
- 필수 필드 존재 여부

```python
class WeatherInput(BaseModel):
    latitude: float = Field(..., ge=-90, le=90)
    longitude: float = Field(..., ge=-180, le=180)
```

---

## 5.4 구조화된 출력

- 모델 응답을 자유 텍스트로만 받으면 파싱 오류가 자주 발생한다
- JSON 형식이나 스키마를 강제하면 후처리가 쉬워진다
- 이번 주에는 "왜 필요한가"를 이해하고, 간단한 예시를 적용하는 수준이면 충분하다

실습 목표:

- 모델이 `{ "city": "...", "action": "..." }` 같은 구조를 따르게 만들기

---

## 5.5 로그와 관찰 포인트

이번 주에 반드시 볼 것:

- 어떤 도구를 선택했는가
- 어떤 입력으로 호출했는가
- 실패는 어디에서 났는가

운영용 관측 플랫폼 전체를 배우기보다, 우선 콘솔 로그만으로도 흐름을 설명할 수 있어야 한다.

---

## 5.6 실습: 날씨 조회 에이전트

### 실습 목표

- 2~3개의 도구를 연결해 간단한 에이전트를 만든다

### 권장 도구 구성

- 도시 이름을 좌표로 바꾸는 도구
- 좌표 기반 날씨 조회 도구
- 결과를 파일로 저장하는 도구

### 실습 흐름

1. 사용자 요청 입력
2. 좌표 조회 도구 호출
3. 날씨 조회 도구 호출
4. 결과 저장
5. 최종 응답 생성

### 학생이 직접 확인할 것

- 도구 선택 순서가 타당한가
- 잘못된 입력에서 어떻게 실패하는가
- 저장된 결과가 사람이 읽기 쉬운가

---

## 제출물

- 도구 호출 에이전트 코드
- 실행 로그 일부
- 정상 요청과 실패 요청 결과 비교
- 짧은 메모
  - 도구 설명을 어떻게 썼는가
  - 어떤 검증을 넣었는가

---

## 체크리스트

- 도구 설명이 모호하지 않다
- 입력 검증이 있다
- 구조화된 출력 또는 JSON 응답을 사용한다
- 최소 한 번 이상 실패 사례를 확인했다
- 로그만 보고도 실행 흐름을 설명할 수 있다

---

## 심화 읽기

- Tool Search
- OpenAI Agents SDK
- 다양한 프레임워크 비교

---

## 다음 주 예고

다음 주에는 LangGraph로 생성-검증-수정 흐름을 제어하는 기본 워크플로우를 만든다.
